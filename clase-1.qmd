# Multiplicación de Matrices y el Espacio Columna

Este capítulo explora la dualidad en la interpretación de la multiplicación matriz-vector y la estructura fundamental de una matriz a través de sus columnas.




## Multiplicación de Matrices por vector $Ax$

La operación fundamental $Ax$, donde $A \in \mathbb{R}^{m \times n}$ y $x \in \mathbb{R}^n$, puede interpretarse de dos maneras fundamentales:

1.  **Perspectiva de Filas:** Cada componente del vector resultante es el producto interno entre una fila de $A$ y el vector $x$.
2.  **Perspectiva de Columnas:** El producto $Ax$ es una **combinación lineal** de las columnas de $A$, donde los coeficientes son las componentes de $x$.

### Definición Formal
Sea $A = [a_1, a_2, \dots, a_n]$ una matriz donde $a_j$ representa su $j$-ésima columna. Entonces:
$$Ax = x_1 a_1 + x_2 a_2 + \dots + x_n a_n$$

Es decir que $Ax$ es una combinacion lineal de las columnas de $A$ definida por los componentes de $x$. Esta interpretación es el pilar central del curso, ya que permite visualizar el producto como un movimiento dentro del subespacio generado por las columnas de la matriz.

Este hecho implica que el vector resultante $b = Ax$ reside necesariamente en el subespacio generado por las columnas de la matriz $A$.




## El Espacio Columna $C(A)$

El **espacio columna** de una matriz $A$, denotado como $C(A)$, es el subespacio de $\mathbb{R}^m$ formado por todas las combinaciones lineales posibles de sus columnas.

**Definición:**
$$C(A) = \{ b \in \mathbb{R}^m : b = Ax \text{ para algún } x \in \mathbb{R}^n \}$$

Geométricamente, si las columnas de una matriz $3 \times 2$ son linealmente independientes, el espacio columna representa un plano en $\mathbb{R}^3$ que pasa por el origen. El sistema de ecuaciones $Ax = b$ tiene solucion si y solo si el vector $b$ pertenece al espacio columna $C(A)$.

El sistema de ecuaciones lineales $Ax = b$ posee solución si y solo si el vector de términos independientes $b$ pertenece al espacio columna de $A$ ($b \in C(A)$) [6].

Si $b \notin C(A)$, el sistema se denomina **incompatible** o inconsistente. En tales casos, el álgebra lineal numérica busca la "mejor aproximación" mediante métodos como mínimos cuadrados, proyectando $b$ sobre el subespacio $C(A)$.




## Independencia Lineal y Rango

El **rango** de una matriz ($r$) se define como el número máximo de columnas (o filas) linealmente independientes. Se dice que una matriz $A \in \mathbb{R}^{m \times n}$ tiene **rango completo** si $r = \min(m, n)$.

### Caracterización mediante Menores Determinantes

Una herramienta analítica para determinar el rango de una matriz es el estudio de sus **menores**. Para una matriz de rango $r$, existe al menos un menor de tamaño $r \times r$ con determinante no nulo.

Observacion: 
*   Si una matriz $A \in \mathbb{R}^{3 \times 2}$ tiene rango 2, al menos uno de sus menores de $2 \times 2$ debe tener un determinante distinto de cero.
*   En una matriz genérica (con valores aleatorios), la probabilidad de que un menor sea singular es cero. Formalmente, en el espacio de matrices $\mathbb{R}^{3 \times 2}$, el conjunto de matrices con rango menor a 2 (donde todos los menores de $2 \times 2$ son singulares) forma una **variedad algebraica**. Dado que los ceros de un polinomio no trivial definen un conjunto de medida cero, la probabilidad de que una matriz elegida aleatoriamente sea singular es nula.





## La Factorización $A = CR$

Toda matriz $A$ de rango $r$ puede descomponerse en el producto de dos matrices más delgadas, $C$ y $R$.

### Teorema de Factorización $A = CR$

Sea $A \in \mathbb{R}^{m \times n}$ una matriz de rango $r$. Existe una matriz $C \in \mathbb{R}^{m \times r}$ cuyas columnas son las primeras $r$ columnas linealmente independientes de $A$, y una matriz $R \in \mathbb{R}^{r \times n}$ tal que:
$$A = CR$$

1.  **Matriz $C \in \mathbb{R}^{m \times r}$:** Se construye seleccionando las primeras $r$ columnas linealmente independientes de $A$..
2.  **Matriz $R \in \mathbb{R}^{r \times n}$:** Contiene los coeficientes necesarios para reconstruir todas las columnas de $A$ a partir de la base $C$. Notablemente, $R$ se encuentra a menudo en forma escalonada reducida (o contiene un bloque identidad $I_r$).


### Propiedades de la Factorización:
1.  **Base del Espacio Columna:** Las columnas de $C$ forman una base para $C(A)$.
2.  **Base del Espacio Fila:** La matriz $R$ contiene una base para el espacio de filas de $A$. 
3.  **Eficiencia de Almacenamiento:** Si $r \ll n$ y $r \ll m$, la matriz puede codificarse utilizando $r(m + n)$ coeficientes en lugar de $mn$, lo cual es crucial en ciencia de datos.

#### Igualdad de dimensiones (Esbozo)
Considerando la descomposición $A = CR$, se observa que cada fila de $A$ es una combinación lineal de las filas de $R$ (los coeficientes de dicha combinación se encuentran en las filas de $C$). Dado que $R$ tiene $r$ filas independientes (específicamente si se encuentra en forma escalonada reducida), el espacio de filas tiene dimensión $r$, igual a la dimensión del espacio de columnas.

### Teorema: Igualdad del Rango por Filas y Columnas
Un pilar del álgebra lineal es que $\dim(C(A)) = \dim(C(A^T)) = r$. La factorización $A=CR$ ofrece una prueba constructiva:
- Las columnas de $C$ son una base de $C(A)$ por construcción ($dim=r$).
- De la igualdad $A = CR$, se observa que cada fila de $A$ es una combinación lineal de las filas de $R$. Como $R$ tiene $r$ filas independientes, el espacio fila tiene dimensión $r$.

### Importancia en la Ciencia de Datos
Esta factorización permite la **reducción de dimensionalidad**. Si una matriz $n \times n$ tiene rango $r \ll n$, se pueden almacenar $2nr$ valores en lugar de $n^2$ [14, 15]. Esto es la base conceptual de técnicas de compresión y de la aproximación de matrices de bajo rango [19].





## Aplicación: Producto Exterior y Matrices de Rango 1

Una matriz de rango 1 puede expresarse como el **producto exterior** de dos vectores $u$ y $v^T$:
$$A = uv^T$$
En esta estructura, el espacio columna es la recta generada por $u$ y el espacio fila es la recta generada por $v$ [20, 21]. Cualquier matriz de rango $r$ puede descomponerse en la suma de $r$ matrices de rango 1 [22, 23]:
$$A = \sum_{i=1}^{r} c_i r_i^T$$
Esta perspectiva es esencial para entender factorizaciones avanzadas como SVD o la diagonalización, donde la información de la matriz se "fragmenta" en componentes de importancia decreciente [19, 24].









